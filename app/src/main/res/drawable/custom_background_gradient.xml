<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Nền -->
    <gradient
        android:angle="270"
        android:startColor="@color/color_a"
        android:endColor="@color/color_b"
        android:centerColor="@color/color_c"
        android:gradientRadius="100"/>

    <!-- Viềm -->
    <stroke
        android:width="3dp"
        android:color="@color/color_a"/>

    <padding
        android:top="0dp"
        android:bottom="0dp"
        android:left="0dp"
        android:right="0dp"/>

    <corners
        android:radius="12dp"/>
</shape>